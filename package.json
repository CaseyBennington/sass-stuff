{
  "name": "sass-course",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run gulp && concurrently \"npm run gulp-watch\" \"npm run lite\"",
    "lite": "lite-server",
    "gulp": "gulp",
    "gulp-watch": "gulp-watch"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.2.0",
    "gulp-sass": "^4.0.2",
    "lite-server": "^2.5.4"
  }
}
